global:
  scrape_interval: 5s # 5초마다 데이터를 긁어감

scrape_configs:
  - job_name: 'my-chat-app'
    metrics_path: '/actuator/prometheus' # 스프링 부트가 열어둔 뒷문 경로
    static_configs:
      # [변경] 분산 서버들의 주소를 모두 적어줍니다.
      - targets: ['chat-app-1:8080', 'chat-app-2:8080']